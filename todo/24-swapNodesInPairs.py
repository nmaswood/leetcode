class ListNode(object):
    def __init__(self, x):
        self.val = x
        self.next = None

class Solution(object):
    def swapPairs(self, head):
        """
        :type head: ListNode
        :rtype: ListNode
        """

        saveHead = head

        while curr:

            saveNext = curr.next
            nextNext = curr.next.next

            curr.next = nextNext













